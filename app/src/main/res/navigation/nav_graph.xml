<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <!-- Home Fragment -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.blazenxt.vibetube.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_home_to_player"
            app:destination="@id/playerFragment"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:popEnterAnim="@anim/slide_in_left"
            app:popExitAnim="@anim/slide_out_right" />
    </fragment>

    <!-- Search Fragment -->
    <fragment
        android:id="@+id/searchFragment"
        android:name="com.blazenxt.vibetube.ui.search.SearchFragment"
        android:label="Search"
        tools:layout="@layout/fragment_search">
        <action
            android:id="@+id/action_search_to_player"
            app:destination="@id/playerFragment" />
    </fragment>

    <!-- Library Fragment -->
    <fragment
        android:id="@+id/libraryFragment"
        android:name="com.blazenxt.vibetube.ui.library.LibraryFragment"
        android:label="Library"
        tools:layout="@layout/fragment_library" />

    <!-- Downloads Fragment -->
    <fragment
        android:id="@+id/downloadsFragment"
        android:name="com.blazenxt.vibetube.ui.downloads.DownloadsFragment"
        android:label="Downloads"
        tools:layout="@layout/fragment_downloads" />

    <!-- Settings Fragment -->
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.blazenxt.vibetube.ui.settings.SettingsFragment"
        android:label="Settings"
        tools:layout="@layout/fragment_settings" />

    <!-- Player Fragment -->
    <fragment
        android:id="@+id/playerFragment"
        android:name="com.blazenxt.vibetube.player.PlayerFragment"
        android:label="Player"
        tools:layout="@layout/activity_player">
        <argument
            android:name="video_id"
            app:argType="string" />
        <argument
            android:name="video_title"
            android:defaultValue=""
            app:argType="string" />
        <argument
            android:name="video_channel"
            android:defaultValue=""
            app:argType="string" />
        <argument
            android:name="video_thumbnail"
            android:defaultValue=""
            app:argType="string" />
    </fragment>

</navigation>
